// Volt, quiet register. Nothing on a recovery screen is live, rewarded,
// or celebrating, so the ground stays void (nebula inherited from body),
// the card stays panel, and the only accent spent is the plasma gradient
// on the one action that gets the user moving again.
export const page = "flex min-h-dvh items-center justify-center px-4 py-10";
export const card = "w-full max-w-sm p-6 text-center";

export const kicker = "kicker text-dust";
export const title = "display text-4xl leading-none mt-2 whitespace-pre-line";
export const body = "text-sm font-semibold leading-snug text-dust mt-3";

export const actions = "flex flex-col items-center gap-3 mt-6";
export const home =
  "border border-edge rounded-full bg-panel px-4 py-2 text-xs font-extrabold uppercase tracking-widest text-dust";

// The digest is for bug reports, not for reading. Dust-dim, tabular, and
// small enough that it never competes with the way out.
export const digest = "text-[10px] font-semibold text-dust-dim mt-4 break-all";
