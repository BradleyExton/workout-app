// The glow rides on the SVG itself (not the coin) so a dim locked glyph
// and a lit unlocked one can share the same container styles.
export const plasma =
  "[filter:drop-shadow(0_0_5px_rgb(255_62_165_/_0.55))]";
export const dim = "";
