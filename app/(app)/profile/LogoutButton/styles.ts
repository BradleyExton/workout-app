export const form = "w-full";
// Volt polish only — the destructive Button variant already carries the
// plasma-pink treatment; this just adds the shared press feedback.
export const button = "active:opacity-70 transition-opacity";
